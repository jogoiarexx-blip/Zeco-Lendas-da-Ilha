{
  "name": "zeco-lendas-da-ilha",
  "version": "1.0.0",
  "description": "Zeco e a Ilha das Gemas — plataforma 2D Phaser PWA",
  "private": true,
  "scripts": {
    "build:www": "node scripts/copy-www.js",
    "cap:init": "npx cap init \"Zeco\" \"com.zecogames.ilha\" --web-dir www",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npm run build:www && npx cap sync android",
    "cap:open": "npx cap open android",
    "cap:build": "npm run cap:sync && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
